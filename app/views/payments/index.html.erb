<h2 align="center">Payment Status</h2>
<div class="table-responsive">          
  <table class="table">
    <thead>
      <tr class="info">
        <th>#</th>
        <th>Amount</th>
        <th>Payment Date</th>

        <th>Due date</th>
        <th>Status</th>
      </tr>
    </thead>
    <tbody>
    <% current_user.payments.each_with_index do |payment, index| %>
        <tr>
          <td><%#= payment.id %><%= index + 1 %></td>
          <td><%= payment.amount %></td>
          <td><%= payment.payment_date %></td>
          <td><%= payment.due_date %></td>
          <td>
            <% if payment.status == 1 %>
            due
            <%else%>
            paid
            <%end%>
          </td>
    <% end %>
    </tbody>
  </table>
</div>